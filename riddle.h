#pragma once

#include "Enums.h"
#include "Point.h"
#include "Screen.h"
#include "utils.h"



#include <cctype> // for isdigit() 
#include <conio.h>  //for _getch()
#include <fstream>

using std::ifstream;
using std::stoi;

class Player;  //forward decleration  
class Game;

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

class riddle
{
	Screen& my_screen;
	Game* my_game;           //for get input from game  
	string my_lastLog = ""; //msg  to report 

	struct my_riddle    //according all paramrters (especially about -x_of_riddle,y_of_riddle,room_of_riddle) 
	{
		int x_of_riddle;
		int y_of_riddle;
		int room_of_riddle;

		int correct_ansewr;
		int index_of_my_arr;

		bool if_sloved_riddle;

	};
	/////////////////////////////////////////////////////////
	vector<my_riddle> my_arr;
	vector<vector<string>> riddles_screens;

	static const char* feedback_screens[3][MAX_Y];
	void loadRiddlesFromFile(const string& filename);
	bool last_answer_is_correct = false;

	////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
public:

	riddle(Screen& screen, Game* game);  //get game* from constractor //


	////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
	my_riddle correctRiddle(Point& p, int my_room); //this function return the correct riddle
	////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

	void run(Point& p, int my_room);
	//we can be sure the the function run just when we need it- because the char '?' delete when player answer correctly
///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

	void draw_riddle(int room) const;

	///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
	void draw_feedback(int room) const;

	//////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
	bool getIfSolved() const
	{
		return last_answer_is_correct;
	}
	///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
	string getLastLog() const
	{
		return my_lastLog;
	}
	///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
};
